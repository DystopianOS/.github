name: "Rotate Weekly"


on:
  workflow_dispatch:
  schedule:
    - cron: '22 3 * * 2' # Every Tuesday at 3:22 AM UTC


jobs:
  rotate-suffix:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    outputs:
      old_suffix: ${{ steps.old_suffix.outputs.oldsuffix }}
    steps:
      - name: Create GitHub App Token
        id: app_token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.DYSTOPIANBOT_APP_ID }}
          private-key: ${{ secrets.DYSTOPIANBOT_KEY }}
          permission-secrets: write
          permission-organization-secrets: write
          owner: ${{ github.repository_owner }}

      - name: Get Old Suffix
        id: old_suffix
        shell: bash
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
        run: |
          echo "oldsuffix=${{ secrets.KEY_SUFFIX || 'XXX' }}" >> "$GITHUB_OUTPUT"

      - name: Update Key Suffix
        id: update_suffix
        shell: bash
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
        run: |
          openssl rand -base64 128 | tr -d '/+=' | head -c 8 | \
            gh secret set "KEY_SUFFIX" \
                      --org "${{ github.repository_owner }}" \
                      --repos "${{ github.repository }},${{ secrets.AUR_DB_REPO_NAME }},${{ secrets.AUR_PKGBUILDS_REPO_NAME }}" \
                      --visibility "selected" || {
              echo "::error::Failed to set KEY_SUFFIX secret" >&2
              exit 1
          }

  rotate:
    runs-on: ubuntu-latest
    needs: rotate-suffix
    permissions:
      contents: write
      actions: write
    steps:
      - name: Create GitHub App Token
        id: app_token
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.DYSTOPIANBOT_APP_ID }}
          private-key: ${{ secrets.DYSTOPIANBOT_KEY }}
          permission-secrets: write
          permission-organization-secrets: write
          owner: ${{ github.repository_owner }}

      - name: New Key
        id: new_key
        shell: bash
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
        run: |
          KEY_NAMES=(
            "${{ format('RANDOM_KEY_{0}0', secrets.KEY_SUFFIX || 'XXX') }}"
            "${{ inputs.key_name || format('RANDOM_KEY_{0}1', secrets.KEY_SUFFIX || 'XXX') }}"
            "###${{ inputs.key_name || format('RANDOM_KEY_{0}2', secrets.KEY_SUFFIX || 'XXX') }}###"
          )

          printf '%s\n' "${KEY_NAMES[@]}" | while IFS= read -r keyname; do
            [ -z "$keyname" ] && continue
            [[ "$keyname" =~ ^\#\#\#.*\#\#\#$ ]] && { keyname=$(printf '%s' "$keyname" | sed -e 's/\#\#\#//g') && l=32 ;} 
            openssl rand -base64 128 | tr -d '/+=' | head -c ${l:-64} | \
              gh secret set "$keyname" \
                        --org "${{ github.repository_owner }}" \
                        --repos "${{ github.repository }},${{ secrets.AUR_DB_REPO_NAME }},${{ secrets.AUR_PKGBUILDS_REPO_NAME }}" \
                        --visibility "selected" || {
                echo "::error::Failed to set new key as secret" >&2
                exit 1
            }
          done
          gh secret list --org "${{ github.repository_owner }}" | awk -F' ' '{print $1}' | grep -E "^RANDOM_KEY_${{ secrets.KEY_SUFFIX }}$" || {
            echo "::error::Failed to verify new key creation" >&2
            exit 1
          }
          
          echo "success=true" >> "$GITHUB_OUTPUT"
          echo "New key(s) set successfully: ${KEY_NAMES[*]}" | sed -e 's/\#\#\#//g'
          

      - name: Delete Old Key
        id: delete_old_key
        if: needs.rotate-suffix.outputs.old_suffix.length > 0 &&
            steps.new_key.outputs.success == 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
          OLD_SUFFIX: ${{ needs.rotate-suffix.outputs.old_suffix }}
        run: |
          KEY_NAMES=(
            "${{ format('RANDOM_KEY_{0}0', needs.rotate-suffix.outputs.old_suffix) }}"
            "${{ inputs.key_name || format('RANDOM_KEY_{0}1', needs.rotate-suffix.outputs.old_suffix) }}"
            "${{ inputs.key_name || format('RANDOM_KEY_{0}2', needs.rotate-suffix.outputs.old_suffix) }}"
          )

          printf '%s\n' "${KEY_NAMES[@]}" | while IFS= read -r keyname; do
            [ -z "$keyname" ] && continue
            [[ "$keyname" =~ ^\#\#\#.*\#\#\#$ ]] && keyname=$(printf '%s' "$keyname" | sed -e 's/\#\#\#//g')
            gh secret delete "$keyname" --org "${{ github.repository_owner }}" || {
              echo "::error::Failed to delete old key '$keyname'" >&2
              exit 1
            }
          done
          
          echo "Old key(s) deleted successfully: ${KEY_NAMES[*]}" | sed -e 's/\#\#\#//g'
